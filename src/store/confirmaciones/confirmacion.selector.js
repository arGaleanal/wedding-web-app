export const selectConfirmacion = (state) => state.confirmacion.confirmacion;

export const selectAllConfirmaciones = (state) => state.confirmacion.confirmaciones;

export const selectConfirmacionError = (state) => state.confirmacion.error;
